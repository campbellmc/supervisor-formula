# -*- coding: utf-8 -*-
# vim: ft=yaml

supervisor:
  config: /etc/supervisord.conf
  epel_sls: ~
  group: root
  legacy: False
  pkg: supervisor
  programs: {}
  program_dir: /etc/supervisord.d
  service_name: supervisord
  user: root
